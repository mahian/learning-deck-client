import React from 'react';

const SectionTitle = ({name}) => {
    return (
        <>
            <h2 className='text-center font-bold text-5xl mb-10'>{name}</h2>
        </>
    );
};

export default SectionTitle;